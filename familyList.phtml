<body>
    <div class="container">
        <div class="tables-layout">
            
            <div class="table-column">
                <h2>Členové tvé rodiny</h2>
                
                <table class="family-table">
                    <thead>
                        <tr>
                            <th>Jméno člena</th>
                            <th>Role</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($clenove as $clen): ?>
                            <tr>
                                <td>
                                    <?php echo htmlspecialchars($clen['username']); ?>
                                </td>
                                <td>
                                    <span class="role-badge">
                                        <?php
                                        if($clen['role_id'] === 1) echo 'Admin';
                                        if($clen['role_id'] === 2) echo 'Rodič';
                                        if($clen['role_id'] === 3) echo 'Dítě';
                                        ?>
                                    </span>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>

                <?php if($uzivatel['role_id'] === 1){ ?>
                    <input type="checkbox" id="modal-toggle" class="modal-toggle-checkbox">
                    <label for="modal-toggle" class="add-member-btn">Přidat člena rodiny</label>

                    <div class="modal modal-add">
                        <div class="modal-content">
                            <label for="modal-toggle" class="close-btn">&times;</label>
                            <h2>Nový člen rodiny</h2>
                            
                            <form action="add_member.php" method="POST" class="modal-form">
                                <label>
                                    Jméno:
                                    <input type="text" name="username" required placeholder="Uživatelské jméno">
                                </label>
                                
                                <label>
                                    Email:
                                    <input type="email" name="email" required placeholder="name@email.com">
                                </label>
                                
                                <label>
                                    Heslo:
                                    <input type="password" name="password" required placeholder="••••••••">
                                </label>

                                <label>
                                    Role:
                                    <select name="role_id" required>
                                        <option value="3">Dítě</option>
                                        <option value="1">Admin</option>
                                        <option value="2">Rodič</option>
                                    </select>      
                                </label>
                                
                                <input type="submit" value="Vytvořit a přidat">
                            </form>
                        </div>
                    </div>
                <input type="checkbox" id="modal-remove-toggle" class="modal-remove-toggle-checkbox">

<label for="modal-remove-toggle" class="remove-member-btn">Odebrat člena rodiny</label>

<div class="modal modal-remove remove-modal">
    <div class="modal-content">
        <label for="modal-remove-toggle" class="close-btn">&times;</label>
        
        <h2>Odebrat člena z rodiny</h2>
        
        <form action="remove_member.php" method="POST" class="modal-form">
            <p style="margin-bottom: 20px; color: #cbd5e1; font-size: 0.95em;">
                Vyber člena, kterého chceš trvale odebrat ze své rodiny:
            </p>
            
            <label>
                Člen rodiny:
                <select name="user_to_remove" required>
                    <option value="" disabled selected>-- Vyber uživatele --</option>
                    <?php foreach ($clenove as $clen): ?>
                        <?php if ($clen['username'] !== $uzivatel['username']): ?>
                            <option value="<?= htmlspecialchars($clen['id']) ?>">
                                <?= htmlspecialchars($clen['username']) ?> (ID: <?= htmlspecialchars($clen['id']) ?>)
                            </option>
                        <?php endif; ?>
                    <?php endforeach; ?>
                </select>      
            </label>
            
            <input type="submit" value="Odebrat z rodiny" class="submit-danger">
        </form>
    </div>
</div>

<div class="modal-wrapper">
    <input type="checkbox" id="modal-edit-toggle" class="modal-edit-toggle-checkbox">
    <label for="modal-edit-toggle" class="edit-member-btn">Upravit člena rodiny</label>

    <div class="modal id-modal-edit">
        <div class="modal-content">
            <label for="modal-edit-toggle" class="close-btn">&times;</label>
            
            <h2>Upravit člena rodiny</h2>
            
            <form action="edit_member.php" method="POST" class="modal-form">
                <p style="margin-bottom: 20px; color: #cbd5e1; font-size: 0.95em;">
                    Vyber člena rodiny a zadej jeho nové údaje:
                </p>
                
                <label>
                    Koho chceš upravit:
                    <select name="user_to_edit" required>
                        <option value="" disabled selected>-- Vyber uživatele --</option>
                        <?php foreach ($clenove as $clen) { ?>
                            <option value="<?= htmlspecialchars($clen['id']) ?>">
                                <?= htmlspecialchars($clen['username']) ?> (ID: <?= htmlspecialchars($clen['id']) ?>)
                            </option>
                        <?php } ?>
                    </select>      
                </label>

                <label>
                    Nové jméno (nebo ponech původní):
                    <input type="text" name="username" placeholder="Nové uživatelské jméno">
                </label>
                
                <label>
                    Nový Email:
                    <input type="email" name="email" placeholder="novy@email.com">
                </label>
                
                <label>
                    Nové Heslo (vyplň pouze pro změnu):
                    <input type="password" name="password" placeholder="••••••••">
                </label>

                <label>
                    Nová Role:
                    <select name="role_id">
                        <option value="" disabled selected>-- Vyber novou roli --</option>
                        <option value="3">Dítě</option>
                        <option value="1">Admin</option>
                        <option value="2">Rodič</option>
                    </select>      
                </label>
                <input type="submit" value="Uložit změny" class="submit-success">
            </form>
        </div>
    </div>
</div>

                <?php } ?>
           