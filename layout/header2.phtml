<!DOCTYPE html>
<html lang="cs">

<head>

    <meta charset="UTF-8">

   <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <title>Pet Scheduler</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="./style.css">

</head>

<?php $theme = $_SESSION['theme'] ?? 'dark'; ?>
<body class="theme-<?= $theme ?>">

<div class="app-layout">

    <!-- SIDEBAR OVERLAY (mobile) -->
    <div class="sidebar-overlay" id="sidebar-overlay"></div>

    <!-- SIDEBAR -->

    <aside class="sidebar" id="sidebar">

        <div class="sidebar-logo">
            <img src="./imgs/logo.png" width="120px" height="40px" alt="">
        </div>

        <nav class="sidebar-menu">

            <a href="./dashboard.php" class="sidebar-link ">
                Dashboard
            </a>

            <a href="./family.php" class="sidebar-link">
                Rodina
            </a>

            <a href="./pets.php" class="sidebar-link">
                Mazlíčci
            </a>

            <a href="./tasks.php" class="sidebar-link">
                Úkoly
            </a>

            <a href="./calendar.php" class="sidebar-link">
                Kalendář
            </a>

        </nav>

        <!-- ADMIN MODE TOGGLE — only shown to role_id=1 -->
        <?php if (isset($uzivatel) && (int)$uzivatel['role_id'] === 1): ?>
        <?php
            $adminMode = $_SESSION['admin_mode'] ?? true;
        ?>
        <div class="sidebar-admin-toggle">
            <span class="sidebar-admin-label">
                <?= $adminMode ? '🛡️ Admin režim' : '👤 Rodičovský režim' ?>
            </span>
            <form action="./toggle_admin_mode.php" method="POST">
                <button type="submit" class="admin-toggle-btn <?= $adminMode ? 'active' : 'inactive' ?>">
                    <?= $adminMode ? 'Přepnout na rodiče' : 'Přepnout na admina' ?>
                </button>
            </form>
        </div>
        <?php endif; ?>

    </aside>

    <!-- PRAVÁ ČÁST -->

    <div class="main-area">

        <!-- TOPBAR -->

        <header class="topbar">

            <!-- Hamburger (mobile only) -->
            <button class="topbar-hamburger" id="sidebar-toggle" aria-label="Otevřít menu">
                &#9776;
            </button>

            <!-- Theme toggle -->
            <form action="./toggle_theme.php" method="POST" style="margin-right: auto;">
                <button type="submit" class="theme-toggle-btn" title="Přepnout motiv">
                    <?php if (($_SESSION['theme'] ?? 'dark') === 'dark'): ?>
                        ☀️
                    <?php else: ?>
                        🌙
                    <?php endif; ?>
                </button>
            </form>

            <details class="user-dropdown">

                <summary class="dropdown-toggle">

                    <div class="user-profile">

                        <svg xmlns="http://www.w3.org/2000/svg"
                             width="18"
                             height="18"
                             viewBox="0 0 24 24"
                             fill="none"
                             stroke="currentColor"
                             stroke-width="2"
                             stroke-linecap="round"
                             stroke-linejoin="round">

                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>

                            <circle cx="12" cy="7" r="4"></circle>

                        </svg>

                        <span class="user-name">

                            <?php
                                echo htmlspecialchars(
                                    $uzivatel['username']
                                    ?? 'Uživatel'
                                );
                            ?>

                        </span>

                    </div>

                </summary>

                <div class="dropdown-menu">

                    <a 
                        href="./account.php"
                        class="dropdown-item-account"
                    >
                        Můj účet
                    </a>
                    
                    <a
                        href="./logout.php"
                        class="dropdown-item"
                    >
                        Odhlásit se
                    </a>

                </div>

            </details>

        </header>

        <!-- CONTENT -->

        <main class="content">
