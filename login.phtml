
<?php
$is_first_visit = !isset($_COOKIE['first_visit_shown']);

if ($is_first_visit) {
    setcookie('first_visit_shown', 'true', [
        'expires' => time() + (30 * 24 * 60 * 60),
        'path' => '/',
        'httponly' => true,
        'samesite' => 'Lax',
        'secure' => true //
    ]);
}
?>
<div class="login-page-wrapper">
    <div class="login-box">
        <h1>Přihlášení</h1>
        
        <form method="post">
            <label>
                Username: 
                <input type="text" name="username" required autocomplete="username">
            </label>
            
            <label>
                Password: 
                <input type="password" name="password" required autocomplete="current-password">
            </label>
            
            <input type="submit" value="Přihlásit se">
        </form>
        <?php if($is_first_visit){?>
        <div class="register-text">
            Nemáš účet? <a href="registrace.php">Zaregistruj se</a>
        </div>
        <?php }?>
    </div>
</div>