<style>
        /* Reset základního nastavení z vaší homepage */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Pozadí zkopírované z vaší sekce .hero */
        body {
            background: linear-gradient(135deg, #1f4068, #162447);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            color: #333;
        }

        /* Karta pro přihlašovací formulář */
        .login-box {
            background-color: #ffffff;
            padding: 2.5rem;
            border-radius: 8px; /* Sjednoceno s obdélníkovějším stylem prvků */
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
            width: 100%;
            max-width: 400px;
        }

        h1 {
            font-size: 2rem;
            color: #162447;
            margin-bottom: 1.5rem;
            text-align: center;
            font-weight: bold;
        }

        /* Stylování chybové hlášky, aby ladila s červeným alertem */
        .error-msg {
            display: block;
            background-color: #fef2f2;
            border-left: 4px solid #ef4444;
            color: #991b1b;
            padding: 0.75rem 1rem;
            border-radius: 4px;
            margin-bottom: 1.25rem;
            font-size: 0.9rem;
        }

        /* Přesné stylování vašich labelů obalujících inputy */
        form label {
            display: block;
            margin-bottom: 1.25rem;
            color: #162447;
            font-size: 0.95rem;
            font-weight: bold;
        }

        /* Vstupní pole uvnitř labelu */
        form label input {
            display: block;
            width: 100%;
            margin-top: 0.5rem;
            padding: 0.75rem 1rem;
            border: 2px solid #e0e0e0;
            border-radius: 5px;
            font-size: 1rem;
            color: #333;
            font-weight: normal;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            outline: none;
        }

        /* Aktivní stav pole při kliknutí - tyrkysová barva z vaší homepage */
        form label input:focus {
            border-color: #00adb5;
            box-shadow: 0 0 0 3px rgba(0, 173, 181, 0.2);
        }

        /* Odesílací tlačítko upravené přesně podle .btn z homepage */
        input[type="submit"] {
            width: 100%;
            background-color: #00adb5;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.1s ease;
            margin-top: 0.5rem;
        }

        /* Hover efekt tlačítka z vaší homepage */
        input[type="submit"]:hover {
            background-color: #007c82;
        }

        /* Klikací efekt */
        input[type="submit"]:active {
            transform: scale(0.98);
        }
    </style>
</head>
<body>
<div class="login-box">
        <h1>Přihlášení</h1>

        <?php if ($chyba) echo "<span class='error-msg'>Chybné přihlašovací údaje!</span>"; ?>
        
        <form method="post">
            <label>Username: <input type="text" name="username" required></label>
            <label>Password: <input type="password" name="password" required></label>
            <input type="submit" value="Přihlásit se">
        </form>
    </div>

